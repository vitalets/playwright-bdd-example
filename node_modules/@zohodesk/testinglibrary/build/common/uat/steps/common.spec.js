"use strict";

var _testinglibrary = require("@zohodesk/testinglibrary");
const {
  Given,
  When,
  Then
} = (0, _testinglibrary.createBdd)();

When('I click link {string}', async ({
  page
}, name) => {
  await page.getByRole('link', {
    name
  }).click();
});