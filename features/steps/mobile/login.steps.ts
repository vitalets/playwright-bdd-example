/**
 * Phase-1 placeholder. The login steps actually used by features/mobile/home.feature
 * are defined in features/steps/shared/generic.steps.ts (since they overlap with
 * other mobile features in subsequent phases). This file exists so the project's
 * step glob has a mobile/ entry on day one; Phase 2 fills in send_money/login_steps.
 */
export {};
